Perfect! I now have complete context. Here's the comprehensive plan with detailed task-by-task instructions for OpenCode.

---

# Theme System Implementation Plan — CSS Variable Themes

## Overview

Create a flexible theme system where changing **one import line** in `BaseLayout.astro` switches the entire visual design.

**Current Architecture Analysis:**
- ✅ `tokens.css` defines all design tokens as CSS variables
- ✅ `global.css` imports tokens and provides shadcn bridge layer
- ✅ `tailwind.config.mjs` extends Tailwind with CSS variables
- ✅ shadcn components use standard token names (`--background`, `--primary`, etc.)

**Strategy:** Create theme files that override the tokens in `:root`, keeping the same variable names but with different values.

---

## Phase Structure

### Phase A: Infrastructure Setup (Tasks A.1 - A.3)
Create the theme system architecture without breaking existing design.

### Phase B: Theme Migration (Tasks B.1 - B.4)
Move current tokens into a theme file and update imports.

### Phase C: Theme Creation (Tasks C.1 - C.3)
Create 3 example themes to demonstrate the system.

### Phase D: Documentation (Tasks D.1 - D.2)
Document the theme system for future theme creation.

---

## Detailed Task Instructions for OpenCode

### TASK A.1: Create Theme Directory Structure

**Objective:** Create the directory structure for themes without modifying existing files.

**Instructions:**

```bash
# Create themes directory
mkdir -p src/styles/themes

# Create README for theme documentation
touch src/styles/themes/README.md
```

**Validation:**
- Directory `src/styles/themes/` exists
- File `src/styles/themes/README.md` exists

**Do NOT proceed to next task until this is complete.**

---

### TASK A.2: Create Base Theme File (Current Design)

**Objective:** Extract current tokens into a theme file called `cinematic-dark.css`.

**Instructions:**

Create file `src/styles/themes/cinematic-dark.css` with this exact content:

```css
/* Theme: Cinematic Dark (Default)
 * LabXR.art - Dark cinematic theme with cyan accent
 */

:root {
  /* Colors - Dark Cinematic Theme */
  --color-bg-primary: #0a0a0a;
  --color-bg-secondary: #1a1a1a;
  --color-bg-tertiary: #2a2a2a;
  --color-text-primary: #ffffff;
  --color-text-secondary: #a0a0a0;
  --color-text-tertiary: #606060;
  --color-accent-primary: #00d4ff;
  --color-accent-secondary: #ff006e;
  --color-accent-tertiary: #ffbe0b;

  /* Spacing Scale */
  --space-xs: 0.25rem;
  --space-sm: 0.5rem;
  --space-md: 1rem;
  --space-lg: 1.5rem;
  --space-xl: 2rem;
  --space-2xl: 3rem;
  --space-3xl: 4rem;

  /* Typography - Font Families */
  --font-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;

  /* Typography - Font Sizes */
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.25rem;
  --text-2xl: 1.5rem;
  --text-3xl: 1.875rem;
  --text-4xl: 2.25rem;
  --text-5xl: 3rem;

  /* Typography - Line Heights */
  --leading-tight: 1.2;
  --leading-snug: 1.3;
  --leading-normal: 1.4;
  --leading-relaxed: 1.5;
  --leading-loose: 1.6;

  /* Typography - Font Weights */
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  /* Border Radius */
  --border-radius-sm: 0.25rem;
  --border-radius-md: 0.5rem;
  --border-radius-lg: 1rem;
  --border-radius-full: 9999px;

  /* Shadows */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.3);
  --shadow-md: 0 4px 6px rgba(0, 0, 0, 0.4);
  --shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.5);
  --shadow-xl: 0 20px 25px rgba(0, 0, 0, 0.6);

  /* Transitions */
  --transition-fast: 150ms;
  --transition-base: 300ms;
  --transition-slow: 500ms;

  /* Z-Index Scale */
  --z-index-dropdown: 1000;
  --z-index-sticky: 1020;
  --z-index-fixed: 1030;
  --z-index-modal-backdrop: 1040;
  --z-index-modal: 1050;
  --z-index-popover: 1060;
  --z-index-tooltip: 1070;
}
```

**Validation:**
- File `src/styles/themes/cinematic-dark.css` exists
- File contains all tokens from current `tokens.css`

**Do NOT proceed to next task until this is complete.**

---

### TASK A.3: Update global.css to Import Theme

**Objective:** Modify `global.css` to import the theme file instead of defining tokens directly.

**Instructions:**

1. Open `src/styles/global.css`

2. Replace the first line:
   ```css
   @import './tokens.css';
   ```
   
   With:
   ```css
   /* Import active theme */
   @import './themes/cinematic-dark.css';
   ```

3. Keep everything else in `global.css` unchanged (shadcn bridge layer, fonts, base styles, utilities).

**Validation:**
- Run `npm run dev` to start dev server
- Visit `http://localhost:4321` in browser
- Site should look **exactly the same** as before
- No console errors
- All colors, spacing, typography unchanged

**If validation fails:**
- Revert changes to `global.css`
- Check that `cinematic-dark.css` has all tokens
- Ensure import path is correct

**Do NOT proceed to next task until validation passes.**

---

### TASK B.1: Remove Old tokens.css

**Objective:** Delete the old `tokens.css` file since its content is now in the theme file.

**Instructions:**

```bash
# Remove old tokens file
rm src/styles/tokens.css
```

**Validation:**
- File `src/styles/tokens.css` does not exist
- Run `npm run dev`
- Site still looks the same
- No import errors in console

**Do NOT proceed to next task until validation passes.**

---

### TASK B.2: Create Minimalist Monochrome Theme

**Objective:** Create a second theme inspired by Linear/Vercel to demonstrate the system.

**Instructions:**

Create file `src/styles/themes/minimal-mono.css` with this content:

```css
/* Theme: Minimalist Monochrome
 * Inspired by Linear, Vercel, Stripe
 * Clean, minimal, professional
 */

:root {
  /* Colors - Light Minimal */
  --color-bg-primary: #ffffff;
  --color-bg-secondary: #fafafa;
  --color-bg-tertiary: #f5f5f5;
  --color-text-primary: #0a0a0a;
  --color-text-secondary: #666666;
  --color-text-tertiary: #999999;
  --color-accent-primary: #0066ff;
  --color-accent-secondary: #0052cc;
  --color-accent-tertiary: #003d99;

  /* Spacing Scale (more generous) */
  --space-xs: 0.25rem;
  --space-sm: 0.75rem;
  --space-md: 1.25rem;
  --space-lg: 2rem;
  --space-xl: 3rem;
  --space-2xl: 4rem;
  --space-3xl: 6rem;

  /* Typography - Font Families */
  --font-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;

  /* Typography - Font Sizes (slightly larger) */
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.375rem;
  --text-2xl: 1.75rem;
  --text-3xl: 2.25rem;
  --text-4xl: 3rem;
  --text-5xl: 3.75rem;

  /* Typography - Line Heights */
  --leading-tight: 1.15;
  --leading-snug: 1.25;
  --leading-normal: 1.4;
  --leading-relaxed: 1.5;
  --leading-loose: 1.65;

  /* Typography - Font Weights */
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  /* Border Radius (more rounded) */
  --border-radius-sm: 0.375rem;
  --border-radius-md: 0.625rem;
  --border-radius-lg: 1.25rem;
  --border-radius-full: 9999px;

  /* Shadows (subtle) */
  --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --shadow-md: 0 2px 8px rgba(0, 0, 0, 0.08);
  --shadow-lg: 0 8px 16px rgba(0, 0, 0, 0.1);
  --shadow-xl: 0 16px 32px rgba(0, 0, 0, 0.12);

  /* Transitions */
  --transition-fast: 150ms;
  --transition-base: 250ms;
  --transition-slow: 400ms;

  /* Z-Index Scale */
  --z-index-dropdown: 1000;
  --z-index-sticky: 1020;
  --z-index-fixed: 1030;
  --z-index-modal-backdrop: 1040;
  --z-index-modal: 1050;
  --z-index-popover: 1060;
  --z-index-tooltip: 1070;
}
```

**Validation:**
- File `src/styles/themes/minimal-mono.css` exists
- File contains all required tokens

**Do NOT proceed to next task until this is complete.**

---

### TASK B.3: Test Theme Switching

**Objective:** Verify that switching themes works by changing one line.

**Instructions:**

1. Open `src/styles/global.css`

2. Change the import from:
   ```css
   @import './themes/cinematic-dark.css';
   ```
   
   To:
   ```css
   @import './themes/minimal-mono.css';
   ```

3. Save the file.

**Validation:**
- Run `npm run dev` (or refresh if already running)
- Visit `http://localhost:4321`
- **Expected result:**
  - Background is now white/light gray
  - Text is dark
  - Accent color is blue (#0066ff)
  - Spacing is more generous
  - Typography is slightly larger
  - Overall feel is cleaner, more minimal
- No console errors
- All sections still render correctly
- shadcn components adapt to new theme

**If validation fails:**
- Check that `minimal-mono.css` has all tokens
- Ensure no typos in variable names
- Verify shadcn bridge layer in `global.css` is intact

**Do NOT proceed to next task until validation passes.**

---

### TASK B.4: Revert to Cinematic Dark Theme

**Objective:** Switch back to the original cinematic dark theme.

**Instructions:**

1. Open `src/styles/global.css`

2. Change the import from:
   ```css
   @import './themes/minimal-mono.css';
   ```
   
   To:
   ```css
   @import './themes/cinematic-dark.css';
   ```

3. Save the file.

**Validation:**
- Refresh browser
- Site looks exactly like the original cinematic dark theme
- All colors, spacing, typography restored

**Do NOT proceed to next task until validation passes.**

---

### TASK C.1: Create Neo-Brutalist Theme

**Objective:** Create a bold, high-contrast theme inspired by Gumroad/Framer.

**Instructions:**

Create file `src/styles/themes/neo-brutalist.css` with this content:

```css
/* Theme: Neo-Brutalist
 * Inspired by Gumroad, Framer, Web3 sites
 * Bold, high-contrast, playful
 */

:root {
  /* Colors - High Contrast */
  --color-bg-primary: #fffbf0;
  --color-bg-secondary: #fff4d6;
  --color-bg-tertiary: #ffe8a3;
  --color-text-primary: #000000;
  --color-text-secondary: #333333;
  --color-text-tertiary: #666666;
  --color-accent-primary: #ff006e;
  --color-accent-secondary: #fb5607;
  --color-accent-tertiary: #ffbe0b;

  /* Spacing Scale (tighter) */
  --space-xs: 0.25rem;
  --space-sm: 0.5rem;
  --space-md: 0.875rem;
  --space-lg: 1.25rem;
  --space-xl: 1.75rem;
  --space-2xl: 2.5rem;
  --space-3xl: 3.5rem;

  /* Typography - Font Families */
  --font-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;

  /* Typography - Font Sizes */
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.25rem;
  --text-2xl: 1.5rem;
  --text-3xl: 1.875rem;
  --text-4xl: 2.5rem;
  --text-5xl: 3.5rem;

  /* Typography - Line Heights */
  --leading-tight: 1.1;
  --leading-snug: 1.2;
  --leading-normal: 1.35;
  --leading-relaxed: 1.45;
  --leading-loose: 1.55;

  /* Typography - Font Weights (bolder) */
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 700;
  --font-weight-bold: 800;

  /* Border Radius (sharp corners) */
  --border-radius-sm: 0;
  --border-radius-md: 0;
  --border-radius-lg: 0;
  --border-radius-full: 9999px;

  /* Shadows (hard, brutal) */
  --shadow-sm: 4px 4px 0px rgba(0, 0, 0, 1);
  --shadow-md: 6px 6px 0px rgba(0, 0, 0, 1);
  --shadow-lg: 8px 8px 0px rgba(0, 0, 0, 1);
  --shadow-xl: 12px 12px 0px rgba(0, 0, 0, 1);

  /* Transitions (snappy) */
  --transition-fast: 100ms;
  --transition-base: 200ms;
  --transition-slow: 300ms;

  /* Z-Index Scale */
  --z-index-dropdown: 1000;
  --z-index-sticky: 1020;
  --z-index-fixed: 1030;
  --z-index-modal-backdrop: 1040;
  --z-index-modal: 1050;
  --z-index-popover: 1060;
  --z-index-tooltip: 1070;
}
```

**Validation:**
- File `src/styles/themes/neo-brutalist.css` exists
- File contains all required tokens

**Do NOT proceed to next task until this is complete.**

---

### TASK C.2: Create Glassmorphism Theme

**Objective:** Create a frosted glass theme inspired by Apple/modern SaaS.

**Instructions:**

Create file `src/styles/themes/glassmorphism.css` with this content:

```css
/* Theme: Glassmorphism
 * Inspired by Apple, modern SaaS
 * Translucent, layered, depth-focused
 */

:root {
  /* Colors - Gradient-ready */
  --color-bg-primary: #0f0f1e;
  --color-bg-secondary: rgba(30, 30, 50, 0.6);
  --color-bg-tertiary: rgba(50, 50, 80, 0.4);
  --color-text-primary: #ffffff;
  --color-text-secondary: rgba(255, 255, 255, 0.8);
  --color-text-tertiary: rgba(255, 255, 255, 0.5);
  --color-accent-primary: #a855f7;
  --color-accent-secondary: #ec4899;
  --color-accent-tertiary: #3b82f6;

  /* Spacing Scale (balanced) */
  --space-xs: 0.25rem;
  --space-sm: 0.625rem;
  --space-md: 1.125rem;
  --space-lg: 1.75rem;
  --space-xl: 2.5rem;
  --space-2xl: 3.5rem;
  --space-3xl: 5rem;

  /* Typography - Font Families */
  --font-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
  --font-mono: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;

  /* Typography - Font Sizes */
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.125rem;
  --text-xl: 1.3125rem;
  --text-2xl: 1.625rem;
  --text-3xl: 2rem;
  --text-4xl: 2.75rem;
  --text-5xl: 3.5rem;

  /* Typography - Line Heights */
  --leading-tight: 1.15;
  --leading-snug: 1.25;
  --leading-normal: 1.4;
  --leading-relaxed: 1.5;
  --leading-loose: 1.6;

  /* Typography - Font Weights */
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  /* Border Radius (very rounded) */
  --border-radius-sm: 0.5rem;
  --border-radius-md: 0.875rem;
  --border-radius-lg: 1.5rem;
  --border-radius-full: 9999px;

  /* Shadows (soft, glowing) */
  --shadow-sm: 0 2px 8px rgba(168, 85, 247, 0.15);
  --shadow-md: 0 4px 16px rgba(168, 85, 247, 0.2);
  --shadow-lg: 0 8px 24px rgba(168, 85, 247, 0.25);
  --shadow-xl: 0 16px 40px rgba(168, 85, 247, 0.3);

  /* Transitions (smooth) */
  --transition-fast: 200ms;
  --transition-base: 350ms;
  --transition-slow: 500ms;

  /* Z-Index Scale */
  --z-index-dropdown: 1000;
  --z-index-sticky: 1020;
  --z-index-fixed: 1030;
  --z-index-modal-backdrop: 1040;
  --z-index-modal: 1050;
  --z-index-popover: 1060;
  --z-index-tooltip: 1070;
}
```

**Validation:**
- File `src/styles/themes/glassmorphism.css` exists
- File contains all required tokens

**Do NOT proceed to next task until this is complete.**

---

### TASK C.3: Test All Themes

**Objective:** Verify all 4 themes work correctly.

**Instructions:**

Test each theme by changing the import in `global.css`:

1. **Test Neo-Brutalist:**
   ```css
   @import './themes/neo-brutalist.css';
   ```
   - Refresh browser
   - Verify: Light cream background, black text, pink accent, sharp corners, hard shadows
   - Check console for errors

2. **Test Glassmorphism:**
   ```css
   @import './themes/glassmorphism.css';
   ```
   - Refresh browser
   - Verify: Dark purple background, white text, purple/pink accents, rounded corners, glowing shadows
   - Check console for errors

3. **Test Minimalist:**
   ```css
   @import './themes/minimal-mono.css';
   ```
   - Refresh browser
   - Verify: White background, dark text, blue accent, generous spacing
   - Check console for errors

4. **Test Cinematic Dark (original):**
   ```css
   @import './themes/cinematic-dark.css';
   ```
   - Refresh browser
   - Verify: Deep black background, white text, cyan accent, original design
   - Check console for errors

**Validation:**
- All 4 themes load without errors
- Each theme has distinct visual identity
- shadcn components adapt correctly
- No layout breaks
- All sections render properly

**Do NOT proceed to next task until all themes pass validation.**

---

### TASK D.1: Create Theme System Documentation

**Objective:** Document how to use and create themes.

**Instructions:**

Update `src/styles/themes/README.md` with this content:

```markdown
# Theme System - LabXR.art

## Overview

LabXR.art uses a CSS Variable-based theme system. Changing **one import line** in `src/styles/global.css` switches the entire visual design.

## Available Themes

| Theme | File | Style | Inspiration |
|-------|------|-------|-------------|
| Cinematic Dark | `cinematic-dark.css` | Dark, cyan accent, cinematic | Default LabXR theme |
| Minimalist Monochrome | `minimal-mono.css` | Light, clean, professional | Linear, Vercel, Stripe |
| Neo-Brutalist | `neo-brutalist.css` | Bold, high-contrast, playful | Gumroad, Framer, Web3 |
| Glassmorphism | `glassmorphism.css` | Translucent, layered, glowing | Apple, modern SaaS |

## How to Switch Themes

1. Open `src/styles/global.css`
2. Find the theme import line:
   ```css
   @import './themes/cinematic-dark.css';
   ```
3. Change it to your desired theme:
   ```css
   @import './themes/minimal-mono.css';
   ```
4. Save and refresh the browser.

## How to Create a New Theme

1. Copy any existing theme file (e.g., `cinematic-dark.css`)
2. Rename it (e.g., `my-custom-theme.css`)
3. Modify the CSS variable values in `:root`
4. Update the import in `global.css` to use your new theme

### Required CSS Variables

Every theme **must** define these variables:

**Colors:**
- `--color-bg-primary` - Main background
- `--color-bg-secondary` - Secondary background (cards)
- `--color-bg-tertiary` - Tertiary background (borders, inputs)
- `--color-text-primary` - Main text color
- `--color-text-secondary` - Secondary text (muted)
- `--color-text-tertiary` - Tertiary text (very muted)
- `--color-accent-primary` - Primary accent (CTAs, links)
- `--color-accent-secondary` - Secondary accent (hover states)
- `--color-accent-tertiary` - Tertiary accent (highlights)

**Spacing:**
- `--space-xs` through `--space-3xl` (7-step scale)

**Typography:**
- `--font-sans` - Sans-serif font stack
- `--font-mono` - Monospace font stack
- `--text-xs` through `--text-5xl` (9-step scale)
- `--leading-tight` through `--leading-loose` (line heights)
- `--font-weight-normal` through `--font-weight-bold` (weights)

**Borders:**
- `--border-radius-sm`, `--border-radius-md`, `--border-radius-lg`, `--border-radius-full`

**Shadows:**
- `--shadow-sm`, `--shadow-md`, `--shadow-lg`, `--shadow-xl`

**Transitions:**
- `--transition-fast`, `--transition-base`, `--transition-slow`

**Z-Index:**
- `--z-index-dropdown` through `--z-index-tooltip` (7-step scale)

## Theme Architecture

```
src/styles/
 ├── themes/
 │   ├── cinematic-dark.css    # Current active theme
 │   ├── minimal-mono.css      # Alternative theme
 │   ├── neo-brutalist.css     # Alternative theme
 │   └── glassmorphism.css     # Alternative theme
 └── global.css                # Imports active theme + shadcn bridge
```

## How It Works

1. **Theme files** define CSS variables in `:root`
2. **global.css** imports the active theme
3. **Tailwind** reads CSS variables via `tailwind.config.mjs`
4. **shadcn bridge** maps custom tokens to shadcn standard names
5. **Components** use Tailwind classes or CSS variables

## Tips for Creating Themes

- **Test contrast ratios** - Ensure text is readable (WCAG AA minimum)
- **Consider dark/light modes** - Some themes work better in dark or light
- **Maintain consistency** - Use the same spacing scale across themes
- **Test all components** - Check buttons, cards, forms, modals
- **Mobile-first** - Test on mobile devices
- **Performance** - Avoid heavy gradients or complex effects

## Future Enhancements

Potential features for the theme system:

- **Runtime theme switcher** - Allow users to switch themes via UI
- **Theme persistence** - Save theme preference in localStorage
- **System preference detection** - Auto-switch based on `prefers-color-scheme`
- **More themes** - Create additional themes (Swiss, Editorial, Cyberpunk, etc.)
- **Theme variants** - Light/dark variants of each theme
```

**Validation:**
- File `src/styles/themes/README.md` exists
- Documentation is clear and complete

**Do NOT proceed to next task until this is complete.**

---

### TASK D.2: Update Project Documentation

**Objective:** Add theme system info to main project docs.

**Instructions:**

1. Open `CONTEXT.md`

2. Find the "Design System" section

3. Add this subsection after the existing design system info:

```markdown
### Theme System

LabXR.art uses a flexible CSS Variable-based theme system.

**Available Themes:**
- `cinematic-dark.css` - Default dark cinematic theme (cyan accent)
- `minimal-mono.css` - Light minimalist theme (blue accent, Linear/Vercel inspired)
- `neo-brutalist.css` - Bold high-contrast theme (pink accent, Gumroad/Framer inspired)
- `glassmorphism.css` - Translucent glowing theme (purple accent, Apple/SaaS inspired)

**How to Switch Themes:**
Change the import in `src/styles/global.css`:
```css
@import './themes/cinematic-dark.css';  // Change this line
```

**Theme Files Location:** `src/styles/themes/`

**Documentation:** See `src/styles/themes/README.md` for complete guide on creating custom themes.
```

4. Open `PROJECT-STATUS.md`

5. Find the "Features" section under "Current State"

6. Add this line:
```markdown
✅ Flexible theme system (4 themes: cinematic-dark, minimal-mono, neo-brutalist, glassmorphism)
```

**Validation:**
- `CONTEXT.md` updated with theme system info
- `PROJECT-STATUS.md` updated with theme feature
- No broken markdown

**Do NOT proceed to next task until this is complete.**

---

## Final Validation Checklist

After completing all tasks, verify:

- [ ] All 4 theme files exist in `src/styles/themes/`
- [ ] `src/styles/tokens.css` is deleted
- [ ] `src/styles/global.css` imports a theme file
- [ ] Each theme can be activated by changing one import line
- [ ] All themes render without console errors
- [ ] shadcn components adapt to each theme
- [ ] Documentation is complete (`src/styles/themes/README.md`)
- [ ] `CONTEXT.md` and `PROJECT-STATUS.md` are updated
- [ ] `npm run check` passes (no TypeScript errors)
- [ ] `npm run build` succeeds

---

## Summary

**What we built:**
- ✅ Theme system architecture with 4 example themes
- ✅ Single-file-switch mechanism (change 1 import line)
- ✅ Complete documentation for creating new themes
- ✅ Zero breaking changes to existing components

**How to use:**
1. Open `src/styles/global.css`
2. Change the theme import line
3. Refresh browser
4. Done!

**Ready for expansion:**
- Create unlimited new themes by copying existing ones
- Each theme controls: colors, typography, spacing, borders, shadows, transitions
- All components automatically adapt

---

**Status:** Plan complete. Ready for OpenCode to execute tasks A.1 through D.2.